# Copy to .env and fill in. Shared across all services via docker-compose.
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars

# Production (Atlas) only — leave blank for local dev:
MONGO_URI_AUTH=
MONGO_URI_MEETINGS=
MONGO_URI_LINKS=
MONGO_URI_PASSWORDS=
MONGO_URI_NOTES=
